# Versiones FIJADAS a propósito.
#
# Con ">=" y sin lockfile, dos instalaciones del mismo commit podían traer
# paquetes distintos: no hay build reproducible, no se puede decir con certeza
# qué versión corre en producción, y una release maliciosa o rota entra sola en
# el siguiente despliegue.
#
# Estas son las versiones verificadas en producción (Python 3.13), auditadas con
# `pip-audit -r requirements.txt` sin vulnerabilidades conocidas.
#
# Para actualizar: subir la versión aquí, ejecutar `pip-audit -r requirements.txt`
# y la suite completa, y solo entonces desplegar.
flask==3.1.3
gunicorn==26.0.0
flask-cors==6.0.5
openpyxl==3.1.5
python-pptx==1.0.2
lxml==6.1.1
requests==2.34.2
beautifulsoup4==4.14.3
google-genai==2.11.0
groq==1.5.0
python-dotenv==1.2.2

# --- Solo para desarrollo y CI (no se instalan en producción) ---
# pytest==8.4.2
# pip-audit
# pyflakes
